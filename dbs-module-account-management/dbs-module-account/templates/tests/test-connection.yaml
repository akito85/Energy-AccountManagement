{{- if .Values.testconnection.enabled -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbs-module-account.fullname" . }}-test-connection"
  labels:
    {{- include "dbs-module-account.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbs-module-account.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
  {{- end }}